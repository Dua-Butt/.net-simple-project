services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "5000"
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8084:8084"
    depends_on:
      - web